odiseo_sylius_easy_post_plugin:
    easy_post_configuration:
        code:
            unique: Ya existe una configuración con este código.
            not_blank: El código no puede ir vacío.
            regex: El código solo puede contener letras, números, guiones y guiones bajos.
        api_key:
            not_blank: La llave de API no puede ir vacía.
            min_length: La llave de API debe tener al menos {{ limit }} caracteres.
            max_length: La llave de API no puede tener más de {{ limit }} caracteres.
